# 3-6-2026 
# Hogg exercise 1.4.27

import random

wins = 0
draws = 0
for i in range(10000):
    x = random.randint(1,20)
    y = random.randint(x,25)
    if y > 21:
        wins += 1
    draws += 1

ratio = wins/draws
print(ratio)

    